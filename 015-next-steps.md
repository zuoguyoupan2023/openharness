# 015 · 规划：OpenHarness 下一步做什么（独立路线图）

> 状态：**规划中（2026-08-17）**
> 范围：本文件**不参考**此前 001–014 的任何规划文档内容，独立基于当前源码现状（`src/`、`src-tauri/`）与开发中遇到的真实痛点提出；旧文档中的规划项不做盘点、不引用。
> 原则：小步、可验证、UI 优先；每项先做出用户看得见的结果再补底层细节（沿用本项目一贯节奏）。

---

## 0. 从链接点击 bug 里提炼的工程教训（先解决这些）

这次「对话里点链接没反应」的根因在 **WebKit 判定层**，桥接层改动两次都无效。教训：

| 教训 | 现状 | 建议 |
|---|---|---|
| 问题层级可能比想象的更低 | 无 | 排查时先做「分流实验」（换环境/换页面/换入口）锁定层级，再动代码 |
| 打包 app 无 devtools，缺结构化日志 | `window.onerror` 只覆盖壳页面 | Rust 侧接 `tauri-plugin-log` 落盘日志；注入脚本探针（本次 `[LINK]` 前缀）保留为通用机制 |
| 链接点击无自动化回归手段 | 全靠手动 | 做一条可重复的「点击 → 断言标签/历史」探针（见 §4 F1），防止再犯 |
| capabilities 只配了 `main` 窗口 | 子 webview 无任何 Tauri 权限 | 未来任何 webview 内 Tauri API 能力前必须补权限设计（见 §4 E） |

---

## 1. 用户可见的功能缺口（按优先级）

### 1.1 P0 · 网页标签的崩溃与下载兜底（稳定性）

**1.1.1 webview 崩溃/白屏自动恢复**
- 现状：DSH 标签有「重启后自动 reload 重连」（`markDshReady`），但**网页标签**加载失败/WebKit 内容进程崩溃（`webContentProcessDidTerminate`）时没有任何兜底，表现为白屏或卡死。
- 做法：Rust 侧 `on_webcontent_process_did_terminate`（wry 已有回调）→ emit 事件 → 前端提示 + 自动 `webview_reload`（失败次数上限后转「打开方式选择」）；`on_navigation` 侧对加载失败（HTTP 错误码/超时）加提示条。
- 规模：S。产出：用户看到「页面崩溃，正在重载…」而不是白屏。

**1.1.2 下载处理**
- 现状：wry `shouldPerformDownload` / 下载响应没有 handler——点下载链接时 `on_navigation` 返回 true（Allow），可能把下载内容当页面导航走，或卡住。
- 做法：Rust 处理 `Download` 导航策略 → 系统下载（`tauri-plugin-opener` 打开下载管理器）或前端下载 UI（进度条 + 完成通知）。
- 规模：M。产出：网页标签里点下载能正常到「下载中/已完成」。

**1.1.3 `window.open` 返回值**
- 现状：`on_new_window` 返回 `Deny`，页面里 `window.open()` 拿到 `null`；部分网站（网银、单点登录弹窗）依赖这个返回值继续交互。
- 做法：Deny 之前先由注入脚本把 JS 语境下的 `window.open` 转成「壳打开」并返回一个**伪 window 引用**（最小 stub），让页面流程不中断。
- 规模：S–M。产出：依赖弹窗的网站不再「点了没后续」。

### 1.2 P1 · 链接与内容体验（延续本次主题）

**1.2.1 追踪域名可配置化**
- 现状：`TRACKING_HOST_RE` 硬编码在 `open-link.ts`。
- 做法：设置页增加「跳转链接自动解析」分组，可增删域名/开关；默认内置现有清单。
- 规模：S。产出：用户能自己控制哪些域名做重定向解析。

**1.2.2 网页标签右键菜单**
- 现状：WebKit 原生右键菜单与壳风格割裂；「复制链接地址 / 在新标签打开」缺失。
- 做法：注入脚本内 `contextmenu` 拦截 → 壳自定义菜单（复用现有 link-dialog 弹层风格）。
- 规模：M。产出：右键体验与壳统一。

**1.2.3 文本选择工具栏**
- 现状：选中网页/对话文本后没有便捷操作。
- 做法：选中出现小浮层：复制 / 搜索（走 `openLink` 统一打开）。
- 规模：M。产出：选中即用。

### 1.3 P1 · 多标签体验补齐

**1.3.1 快捷键**
- 现状：只能鼠标操作。
- 做法：`Cmd/Ctrl+T` 新标签、`Cmd+L` 聚焦地址栏、`Cmd+W` 关闭当前标签、`Cmd+1..9` 切换。
- 规模：S。产出：键盘可用。

**1.3.2 标签固定 / 拖拽排序 / 右键菜单**
- 现状：标签栏只支持 新建/关闭/切换/激活历史标签。
- 做法：固定（钉住，不随会话变动）、拖拽排序持久化、标签右键（刷新 / 复制网址 / 关闭其它）。
- 规模：M。产出：Chrome 级标签操作。

**1.3.3 历史记录增强**
- 现状：按天分组 + 清空，无搜索、无单条删除。
- 做法：顶部搜索框（URL/标题过滤）、单条删除、`Cmd+H` 快捷打开。
- 规模：S–M。产出：历史可用性提升。

### 1.4 P2 · 现代化与增长

**1.4.1 网页标签拖出为独立窗口**
- 现状：单窗口内多标签。
- 做法：标签拖出 → 新建 Tauri 窗口承载该 webview（`WebviewWindow`）；拖回合并。
- 规模：L。产出：双屏/多窗口工作流。

**1.4.2 全屏 / 画中画（PiP）**
- 做法：网页标签一键全屏；视频画中画按钮。
- 规模：M。

**1.4.3 会话与工作区**
- 现状：标签持久化到 localStorage，无多场景。
- 做法：会话快照（标签组命名保存/恢复）、按项目隔离标签组。
- 规模：L。

**1.4.4 状态栏**
- 做法：底部状态栏：下载进度、background job 计数（若 DSH 暴露）、代理状态、内存占用提示。
- 规模：M。

---

## 2. 多平台与发布

| # | 事项 | 现状 | 做法 | 规模 |
|---|---|---|---|---|
| 2.1 | **Windows / Linux 适配** | 仅 macOS 目标 | WebView2 的 `on_new_window` 语义、缩放坐标（`LogicalPosition` 高分屏）、进程回收的 `kill(-pgid)` 在 Windows 的等价物（`taskkill /T`）、PTY（portable-pty 已跨平台）逐平台实测 | L |
| 2.2 | **签名 / 公证 / 自动更新** | 未打包发布 | Developer ID + notarize；`tauri-plugin-updater` 建发布管道 | M |
| 2.3 | **CI 构建矩阵** | 无 | GitHub Actions：macOS/Windows/Linux `cargo tauri build` + 上传产物 | M |

---

## 3. 安全与隔离

| # | 事项 | 现状 | 做法 | 规模 |
|---|---|---|---|---|
| 3.1 | **capabilities 收敛** | 仅 `main` 窗口有权限 | 定义子 webview 能力边界；远程内容（网页标签）默认零权限，仅 DSH 标签可开白名单能力 | S |
| 3.2 | **注入面最小化** | 注入脚本 1 个（链接兜底） | 审计注入脚本数量与页面；新增注入一律走单一入口注册 | S |
| 3.3 | **下载安全** | 无下载处理 | 危险类型（可执行/脚本）默认拦截提示 | S |

---

## 4. 工程与质量

| # | 事项 | 做法 | 规模 |
|---|---|---|---|
| 4.1 | **链接点击回归探针（e2e）** | 注入脚本把「每次链接点击」写成结构化日志（href/target/时间/是否被兜底）；测试脚本断言：点击后历史有记录、标签出现、追踪链接解析正确。把本次 bug 的三个场景固化成自动化 | M |
| 4.2 | **结构化日志落盘** | `tauri-plugin-log`：Rust 侧 nav/new-window/title/崩溃事件全部带时间戳落盘；打包后可从「日志视图」导出 | S |
| 4.3 | **构建验证流程固定** | `cargo check`（rustup stable）+ `tsc --noEmit` + `cargo tauri build` 一键脚本；本次修复暴露「Homebrew rustc 1.86 与 rustup stable 1.97 混用」坑，写入 README | S |

---

## 5. 建议实施顺序（三轮）

- **第一轮（P0，稳定性）**：1.1.1 webview 崩溃恢复 → 1.1.2 下载处理 → 4.2 结构化日志。
- **第二轮（P1，体验）**：1.2.1 追踪域名配置 → 1.3.1 快捷键 → 1.3.2 标签固定/排序 → 1.2.2 右键菜单。
- **第三轮（P1/P2）**：1.2.3 文本选择工具栏 → 1.3.3 历史搜索 → 4.1 回归探针 → 2.1 Windows/Linux 适配启动。

> 每轮结束按本项目惯例：写入对应 `0xx` 文档（含手动验证矩阵）+ 用户确认后再进下一轮。