# 固定 Rust 工具链：tauri-plugin-dialog 依赖树要求 rustc ≥ 1.88。
# 本仓库根/工作区若用 Homebrew rustc 1.86，dialog 无法编译；
# 此项让 src-tauri 内构建自动使用 rustup 的 stable（本机 1.97，≥ 1.88）。
# 若本机尚无 stable，请先 `rustup toolchain install stable`。
[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
