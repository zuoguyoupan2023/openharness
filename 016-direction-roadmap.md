# 016 · 方向与路线：从「DSH 壳」到「AI 桌面工作台」

> 项目：`openharness`（Tauri v2 + Vite + vanilla TS）· 更新日期：2026-08-17
> 本文是**独立的方向规划**——基于 [`001-summary.md`](001-summary.md) 的现状盘点，重新思考"接下来做什么、往哪个方向走"，不参考任何过往规划文档。
> 一句话定位：**openharness 不是"装 DSH 的壳"，而是"以 DSH 为引擎的 AI 桌面工作台"。**

---

## 〇、核心判断

DSH 本身已经很强：对话、Agent、工具执行、headless、插件体系——它缺的不是能力，而是**一个让人愿意用、能顺利用起来的"产品外壳"**。openharness 的价值恰恰在浏览器给不了的地方：进程托管、系统集成、原生体验、离线与分发。

因此接下来的主线不是"再包一层壳"，而是回答三个问题：

1. **装得上、跑得稳**（工程地基）：当前最大的体验风险是首次启动要 `npx` 现场下载 DSH、依赖用户自装 Node ≥22、无签名无自动更新——**这一步不过关，其它功能都是白做**。
2. **拿起来就会用**（产品化）：非开发者打开后不知道能干什么——需要引导、预设、模板、上下文注入，让"第一份成果"在 5 分钟内出现。
3. **离不开**（原生增值）：全局快捷键、菜单栏、通知、Finder 集成、拖放注入——浏览器做不到，才是壳的不可替代性。

下面按 7 个方向展开，最后给出推荐路线（P0→P3）与明确"不做"的清单。

---

## 一、方向总览

| # | 方向 | 一句话 | 对应问题 |
|---|------|--------|----------|
| 1 | **Runtime 受管化** | 从 `npx 现下载` 升级为「版本锁定 + 缓存 + 离线 + 看门狗」 | 装不上、跑不稳 |
| 2 | **首次引导与场景化** | 3 步向导 + 场景套装 + 模板，让新用户 5 分钟出成果 | 不会用、不知道能干嘛 |
| 3 | **会话与上下文产品化** | 拖放注入、会话管理、结果落地为文件 | 与本地文件系统割裂 |
| 4 | **系统级集成** | 全局快捷键、菜单栏常驻、通知、Finder 扩展、截图提问 | 浏览器给不了的原生体验 |
| 5 | **插件生态深化** | 配置表单化、权限与安全、运行状态、场景套装 | 143 个插件只能"凭名字猜" |
| 6 | **分发与更新闭环** | 签名公证、自动更新、多安装渠道、崩溃上报 | 别人装不上、不敢装 |
| 7 | **信任与隐私** | 本地优先承诺、可选云同步、供应链校验 | 非开发者用户的信任门槛 |

---

## 二、方向详解

### 方向 1：Runtime 受管化（地基，最高优先）

**要解决的问题**：当前 `start_dsh` 是 `spawn npx --yes @deepseek-ai/dsh web`——每次拉起都可能重新解析版本、受网络抖动影响；首次使用要等下载；无版本锁定；无法离线；数据目录无管理。这是"让更多人用"的第一个拦路虎。

**做什么**：

1. **受管 DSH 运行时**：安装时把 DSH CLI 缓存到 app 目录（类似 pnpm store），启动不再依赖 npx 网络解析；`dsh --version` 与远端比对，**锁定/回滚版本**，界面显示当前版本与可用版本。
2. **首次安装体验**：下载进度界面 + 断点续传 + 已有 npm 镜像回退；下载完成前不白屏。
3. **数据目录管理**：定位 DSH 的 config/state 目录，提供**备份 / 导出 / 迁移 / 重置**入口（设置页），避免用户"换电脑 = 丢会话"。
4. **看门狗**：DSH 进程异常退出自动重启（区分：用户手动关闭不重启、崩溃/被杀自动拉起并提示）。
5. **端口策略**：外部实例仍直连（现状保留），但可更进一步——把外部实例也纳入"运行状态"展示，避免用户困惑"为什么有两个 DSH"。

**价值**：稳定性 = 口碑。这条不做，其它所有方向的体验都建立在流沙上。

---

### 方向 2：首次引导与场景化（获客，第一印象）

**要解决的问题**：现在装好打开就是 DSH 的裸对话界面。开发者知道怎么配模型、怎么问；**非开发者打开就懵**。

**做什么**：

1. **首次启动向导（3 步）**：
   - 第 1 步：选身份/场景（写代码 / 写作研究 / 学习翻译 / 通用）；
   - 第 2 步：模型配置引导（填 API Key / 选服务商，链到 DSH 设置）；
   - 第 3 步：按场景推荐插件组合，一键安装（复用现有预装/安装链路）。
2. **场景套装（Plugin Suite）**：像"应用商店的 App"一样，一个套装 = 一组插件 + 一组模板，如"代码审查工作台""论文写作助手"。一键装好即用。
3. **内置模板库**：侧边栏提供可插入的提示词模板（按场景分类），用户可以改、可以存自己的。
4. **空状态引导**：对话为空时显示"试试这些"示例任务（如"总结这个文件夹"），点了就自动注入上下文并发送。
5. **内置帮助**：FAQ + 常见问题排查（Node 缺失怎么装、镜像怎么配、日志怎么看），直接在 app 内可查。

**价值**：拉新与留存的关键——新用户第一次成功产出，比任何功能都重要。

---

### 方向 3：会话与上下文产品化（工作台体验）

**要解决的问题**：现在对话都发生在 DSH web 页面里，与本地文件系统割裂：不能拖文件进去、产物不能一键落盘、会话不能导出分享。

**做什么**：

1. **拖放注入**：把文件 / 文件夹 / 图片 / URL 拖进对话窗口 → 自动转成上下文内容（结构：文件名 + 内容摘要 + 链接），DSH 侧以可用的形式注入。
2. **会话管理**：会话重命名、归档、搜索；导出为 Markdown / JSON；删除前可备份。
3. **上下文包（Context Pack）**：一键把"当前文件夹结构 + 选中文件 + 剪贴板 + 说明"打包成一个注入单元，适合"帮我 review 这个项目"类任务。
4. **结果落地**：对话产出的代码/文档/表格 → 一键写入本地文件（自动识别代码块语言与文件名命名建议）。
5. **（远期，可选）会话云同步**：端到端加密，跨设备续聊（归入方向 7 的信任体系）。

**价值**：把"浏览器里的对话"变成"工作台上的助手"——这一步是"壳"变成"工作台"的标志。

---

### 方向 4：系统级集成（原生增值，差异化）

**要解决的问题**：用户打开浏览器也能用 DSH，凭什么用 openharness？答案：**浏览器做不到的事**。

**做什么**（按性价比排序）：

1. **全局快捷键唤起**：⌘⇧Space 从任意 App 唤起快速提问窗口；**选中文字即问**（选中 → 快捷键 → 上下文自动带上）。
2. **菜单栏常驻模式**：不占 Dock，长任务（如 Agent 跑批）后台运行，完成时**系统通知 + 声音**。
3. **系统通知**：任务完成 / 出错 / 需要确认时推送（含进度）。
4. **Finder 扩展 / 右键菜单**：文件或文件夹右键 → "发送到 openharness 提问"。
5. **截图提问**：全局截图（复用系统截图能力）→ 图片自动进对话（配合多模态模型）。
6. **剪贴板历史**：壳内管理复制历史，一键插入对话。
7. **拖放**（与方向 3 联动，窗口级支持）。

**价值**：形成"日常伴侣"的使用习惯——用户不是"打开 DSH"，而是"随时在跟 openharness 说话"。这是别处抄不走的护城河。

---

### 方向 5：插件生态深化（从"能装"到"好用可信"）

**要解决的问题**：插件中心已有 143+ 社区插件，但用户只能"凭名字猜"：装完不知道配置了什么、是否安全、是否在跑。

**做什么**：

1. **插件配置表单化**：安装后给出可视化的配置界面（API Key、参数、开关），而不是让用户去查文档改环境变量。
2. **权限与安全模型**：插件声明所需权限（网络 / 文件 / 执行命令），安装前展示；可疑插件（权限过大、来源不明）黄色警告。
3. **场景套装**（与方向 2 联动）：套装页 = 一键装一组插件 + 模板。
4. **运行状态面板**：某插件是否在跑、近 7 天调用次数、耗时、token 消耗——让"装插件"有反馈。
5. **本地开发工具链（远期）**：内置插件脚手架，可视化开发/调试，一键发布到集市。

**价值**：插件从"清单"变成"生态"。装插件的人越多、越可信，DSH 越强，壳越有存在价值。

---

### 方向 6：分发与更新闭环（让别人真的能装上）

**要解决的问题**：当前无签名、无公证、无自动更新、只有 macOS——用户下载后可能被 Gatekeeper 拦截，更新只能手动重新打包。**这直接卡死"更多人使用"。**

**做什么**：

1. **签名 + 公证（notarization）**：消除 Gatekeeper "无法验证开发者"弹窗。
2. **自动更新（Sparkle 或 tauri-updater）**：静默后台更新 + 变更说明，粒度到"D 级可用"。
3. **多安装渠道**：.dmg 直下 + Homebrew Cask；Windows 走 Winget / MSIX，Linux 走 AppImage / deb（跨平台详见备注）。
4. **崩溃上报与匿名统计（默认关闭、明确告知）**：装不上、跑不起来的数据，比任何用户访谈都真实。
5. **CI 发布流水线**：tag → 构建三平台 → 签名 → 上传 → 更新 manifest。

**价值**：没有它，"方向 2 的引导做得再好"也到不了用户手里。

> 备注：跨平台（Windows/Linux）是一个独立的大工程（webview 差异、路径、权限、打包），建议在 macOS 验证完产品价值之后再投入，故不入 P0 列表，见下节路线。

---

### 方向 7：信任与隐私（面向普通用户的承诺）

**要解决的问题**：开发者不在乎数据在哪；普通用户在乎。想让"更多人"用，必须把"本地优先"变成可感知的承诺。

**做什么**：

1. **本地优先声明**：设置页 + 官网写明"对话与数据默认全部留在本机"。
2. **（可选）端到端加密云同步**：会话跨设备，加密在客户端完成，服务方不可见（远期）。
3. **供应链校验**：DSH runtime 与插件下载后的完整性校验（对 registry 源的 hash 校验），防止 npm/镜像源被污染时静默注入恶意包。
4. **权限可视化**：把"这个插件要访问什么"清楚地展示（与方向 5 联动）。

**价值**：信任是"更多人使用"的隐形地板——不是功能，但缺了它，上面所有方向都会打折。

---

## 三、推荐路线（P0 → P3）

> 原则：**先让产品"装得上、用得稳"，再做"让新用户上手"，再做"让人离不开"，最后做"生态与信任"**。每一阶段都有可交付、可见的成果，做完即可对外发版。

| 阶段 | 内容 | 交付物（用户可见） | 优先级理由 |
|------|------|--------------------|------------|
| **P0 地基** | 方向 1（Runtime 受管化）+ 方向 6 的签名/公证/自动更新 | 首次启动有下载进度；版本可锁定/回滚；换机可备份迁移；Gatekeeper 不再拦截；更新自动完成 | 现在最大的流失点就在"装不上/跑不稳" |
| **P1 上手** | 方向 2（引导与套装）+ 方向 3（拖放注入与会话管理） | 3 步向导、场景套装、模板库、空状态示例、拖文件进对话、产物一键落盘 | 决定"第一次用完还来不来" |
| **P2 增值** | 方向 4（系统集成） | 全局快捷键、菜单栏常驻、通知、Finder 右键、截图提问 | 形成差异化与使用习惯 |
| **P3 生态** | 方向 5（插件生态）+ 方向 7（信任） | 插件配置表单、权限提示、运行状态面板、本地优先声明、供应链校验 | 规模化的护城河；跨平台并入此阶段评估 |

**建议的推进节奏**：P0 独立成版（v0.2.0）；P1 与 P2 可并行（各自的模块互不依赖）；P3 放长线，随社区插件数量增长滚动迭代。

---

## 四、明确不做（防御清单）

1. **不重做 DSH 的核心 AI 能力**：对话引擎、Agent 编排、工具执行是 DSH 的事，壳只做"产品化 + 集成"。任何"壳里再造一个对话"的想法都拒绝。
2. **不做模型中转 / 自建 API**：模型接入完全跟随 DSH 的能力。
3. **初期不做复杂账号体系**：本地优先，不注册不登录；云同步设为可选远期项。
4. **不把壳做成通用浏览器**：多标签浏览是辅助能力，重点永远是"DSH 工作台"；不为网页浏览投入专项优化。
5. **不卷"特效 UI"**：保持现在的轻量 vanilla 技术栈，稳定优先；界面美化的投入只服务于"上手更快"。

---

## 五、成功指标（怎么算"更多人使用"）

| 指标 | 目标 | 对应方向 |
|------|------|----------|
| 首次启动失败率 | 从"偶发"降到 <2%（含无 Node 环境也能装运行时） | 方向 1 |
| 激活率（安装后真正跑起来） | ≥90% | 方向 1、6 |
| 首次 24h 留存 | ≥40% | 方向 2、3 |
| 周活 / 下载比 | 持续上升 | 方向 2、3、4 |
| 场景套装安装率 | 新用户 ≥30% 装至少 1 个套装 | 方向 2、5 |
| 崩溃率 | <0.5% | 方向 1、6 |
| 插件安装覆盖率 | 活跃用户 ≥50% 装过插件 | 方向 5 |

---

## 六、一句话总结

**openharness 的下一步，是把"能打开 DSH 的壳"升级成"装得上、学得会、离不开的 AI 桌面工作台"——顺序是：先修地基（受管 Runtime + 分发闭环），再做上手（引导 + 上下文），再建护城河（系统集成），最后养生态（插件 + 信任）。**

---

## 七、需求登记（2026-08-17 补充）

> 用户提出的具体需求登记在此，按提出的先后排序，不并入上文方向篇章（避免打散主线）。

| # | 需求 | 现状 | 期望 | 备注 |
|---|------|------|------|------|
| R1 | **默认标签页（官方对话站）** | 启动后只有一个 DSH 标签（`http://127.0.0.1:3080`） | 启动时**额外自动打开**一个标签页 `https://chat.deepseek.com`（DeepSeek 官方对话页），但**默认显示的仍是 3080 的 DSH 窗口** | 复用现有原生 webview 多标签机制：`dsh-ready` 后在标签栏追加一个指向 chat.deepseek.com 的网页标签（后台静默创建，不抢占焦点）。用户可手动关闭该标签；关闭后是否自动重建（或提供"恢复默认标签"开关）待定 |

**实现要点（R1）**：
1. 在 `tabs.ts` 的 DSH 就绪回调（`dsh-ready`）中，追加一次 `addTab("https://chat.deepseek.com")`（后台创建，不切换激活标签）；
2. 确保新标签的打开方式不受 `open-link` 默认行为影响（这是启动时主动创建的标签，不是用户点击链接）；
3. 若用户手动关闭过该标签，本次会话内不重复创建；持久化"是否创建默认标签"开关到 localStorage（`localStorage` 已有持久化管线，可复用）。